宽字符类型wchar_t
#include<locale>
setlocale(LC_ALL,"chs");
第一个是设置 包括金融货币、小数点、时间日期格式、语言、字符串的使用习惯//chs中文简体
wchar_t wt[] = L"汉";
//L表示通知编译器，该变量给他分配两个自己的空间
//由于中是个汉字，所以我们需要调用一个函数来将本机的语言设置为中文简体

wcout<<wt;
int 四个字节
short int 两个字节
long 占四个字节，固定的
#include<iomanip>
setprecision()该函数用来测试输出的精度

/*移动摄像头：alt+鼠标
旋转摄像头：ctrl+鼠标*/

指针在不用的时候一定要进行释放delete
指针想减的结果是int类型的  

const 在指针类型前面是指向常量的指针
在类型和* 号之后的const表示常量指针
explicit 关键字加载构造函数之前用来关闭隐式转换
用已有对象来创建对象时，才会调用复制构造函数

const对象只能调用const成员函数
sizeof(a),求数组a字节大小
数组大小int size = sizeof(a)/sizeof(int);