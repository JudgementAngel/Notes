向量点击的几何意义是：一个向量在另一个向量上的共线程度
正交矩阵的逆等于转置

1、Unity 贴图的offset的数值是以一张贴图为单位的，如果平铺多次，offset为1的时候就只平移一张图片的位置，而不是整个UV大小
2、Unity 5.5.0版的Shader的_Time变量在不运行编辑器的情况下也能进行变化
3、//Shader函数UnpackNormal(col)是将Color颜色值变成法线值，但是如果本来就是法线贴图的颜色，在进行这样的计算反而会出错