如果你的程序没有使用size()函数，那么维度就默认设定为100*100

当一个程序运行时，计算机从第一行代码开始逐行执行，直到它遇到最后一行结束。

如果你希望一个图形花在其他所有的图形之上，那么你需要把绘制它的代码放在绘制其他所有图形的代码之后

一些Processing的实现（比如JavaScript版本中的）会默认划出平滑的线，而其他一些也许根本不支持滤波功能。

有些情况下，我们不能在draw()函数中打开或者关闭滤波功能。事实上，3.3版本之后在同一个Sketch中用

像rect()和ellipse()这样设置形状的函数，是通过rectMode()和ellipseMode()函数来控制的。详情见参考文档

注意不要同时禁用 画笔stroke 和 填充fill 这样做的结果就是屏幕上什么都不会出现

Processing拥有一系列特别的变量，在程序运行的时候用来存储信息。比如窗口的宽高是存储在叫做width和height的变量中的，这些值通过size()函数来设定的

stroke描边颜色

processing 可以读取和显示JPEG,PNG,GIF格式的光栅图像
GIF和PNG图像支持透明度
GIF中只有1位的透明度，也就意味着它的像素只有透明和不透明两种状态
PNG有8位透明度

创建字体的时候，字体的大小会影响字体文件的大小，直接通过上面工具创建的字体好像没发现时中文

processing不支持所有SVG的特性，请参考Processing手册中的PShape来获得更多信息

processing一般以每秒60帧的速度运行程序，但是如果draw()函数中的代码运行的时间超过了1/60秒
那么帧率就会下降。frameRate()函数只是设置了最大的帧率，任何程序上的实际帧率都依赖运行代码的计算机性能

randomSeed()函数是用来生成random()函数的种子，如果种子相同，那么每次程序运行生成的随机数序列是相同的

lights()函数能创建一个默认的环境和定向光。灯光每次重置需要通过draw()，所以它们应该出现在draw()的顶部以确保结果一致

由Processing程序创建的动画图像可以通过savaFrame()这个功能转换成一系列文件。
当saveFrame()出现在draw()的末端时，它根据序列TIFF格式保存程序的图片到草稿的文件夹中，
并分别命名为screen-0001.tif、screen-0002.tif等。
这些文件可以被导入成一个视频或者动画程序，并保存为影片文件。
可以用SaveFram("frames/output-####.png");自己重命名，#表示序号


magnet:?xt=urn:btih:63AD215801D3A91AE4B1658F557D29F0E8AE6AF3
